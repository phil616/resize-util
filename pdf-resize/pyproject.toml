[project]
name = "pdf-resize"
version = "0.1.0"
description = "光栅化 PDF 并压缩到指定体积上限，支持多页与统一页面尺寸"
readme = "README.md"
license = "MIT"
authors = [{ name = "phil616" }]
requires-python = ">=3.12"
dependencies = [
    "img2pdf>=0.6.3",
    "pillow>=10.0.0",
    "pymupdf>=1.27.2.3",
]
