[project]
name = "image-resize"
version = "0.1.0"
description = "通用图像有损压缩：任意格式 -> JPEG 压缩 -> 转回原格式，以压小体积为第一目标"
readme = "README.md"
license = "MIT"
authors = [{ name = "phil616" }]
requires-python = ">=3.12"
dependencies = [
    "pillow>=10.0.0",
]
